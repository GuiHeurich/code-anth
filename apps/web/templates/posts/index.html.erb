<h2>Writings</h2>

<% if posts.any? %>
  <div id="posts">
    <% posts.each do |post| %>
    <div class="post">
      <a class="post" href="/posts/<%= post.id %>"><h3><%= post.title %></h3></a>
    </div>
    <% end %>
  </div>
  <% else %>
<p class="placeholder">Nothing to show yet.</p>
<% end %>
